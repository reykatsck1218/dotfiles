# Restarting
alt + shift - r : yabai --restart-service && skhd --restart-service

# Move focus with vim keys
alt - h : yabai -m window --focus west
alt - j : yabai -m window --focus south
alt - k : yabai -m window --focus north
alt - l : yabai -m window --focus east

# Move window position with vim keys
alt + shift - h : yabai -m window --warp west
alt + shift - j : yabai -m window --warp south
alt + shift - k : yabai -m window --warp north
alt + shift - l : yabai -m window --warp east

# Move window to other workspaces
alt + shift - 1 : yabai -m window --space 1;
alt + shift - 2 : yabai -m window --space 2;
alt + shift - 3 : yabai -m window --space 3;
alt + shift - 4 : yabai -m window --space 4;
alt + shift - 5 : yabai -m window --space 5;
alt + shift - 6 : yabai -m window --space 6;
alt + shift - 7 : yabai -m window --space 7;
alt + shift - 8 : yabai -m window --space 8;
alt + shift - 9 : yabai -m window --space 9;
alt + shift - 0 : yabai -m window --space 10;

# Fake fullscreen
alt - f : yabai -m window --toggle zoom-fullscreen

# fullscreen
alt + shift - f : skhd -k "ctrl + cmd - f"

# Toggle floating
alt - v : yabai -m window --toggle float --grid 8:8:1:1:6:6

# Quit/Close window
alt - q : yabai -m window --close

# Some common shortcuts
alt - c : alacritty
alt - e : open ~
alt - b : open https://codedmaster.com

# Alt Tab
alt - tab : yabai -m window --focus next
alt + shift - tab : yabai -m window --focus prev

# Lockscreen
alt - m : skhd -k "ctrl + cmd - q"

# Screenshots
alt - p : skhd -k "cmd + shift - 4"

# Raycast mapping
alt - space : skhd -k "cmd - space"

# Delete
alt - backspace : skhd -k "cmd - backspace"

# CONTROL EMULATION
ctrl - c [
    *           : skhd -k "cmd - c"
    "alacritty" ~
]
ctrl - v [
    *           : skhd -k "cmd - v"
    "alacritty" ~
]
ctrl - r [
    *           : skhd -k "cmd - r"
    "alacritty" ~
]
ctrl - t [
    *           : skhd -k "cmd - t"
    "alacritty" ~
]
ctrl - z [
    *           : skhd -k "cmd - z"
    "alacritty" ~
]
ctrl - y [
    *           : skhd -k "cmd - y"
    "alacritty" ~
]
ctrl - x [
    *           : skhd -k "cmd - x"
    "alacritty" ~
]
ctrl + shift - v [
    *           : skhd -k "cmd + shift - v"
    "alacritty" : skhd -k "cmd - v"
]
ctrl - a : skhd -k "cmd - a"
ctrl - f : skhd -k "cmd - f"
ctrl - n : skhd -k "cmd - n"
ctrl - w : skhd -k "cmd - w"
ctrl + shift - n : skhd -k "cmd + shift - n"
ctrl + shift - t : skhd -k "cmd + shift - t"
ctrl + shift - z : skhd -k "cmd + shift - z"

# Emulate ctrl + shift - i
ctrl + shift - i : skhd -k "cmd + alt - i"
